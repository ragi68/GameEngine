TRANSFORMS


glm::vec3(position) -> glm::vec4(position, 0) + glPosition(verts) OR glm::transform(transofmrvector * vec3(1.0f), verts(?))
glm::scale(verts, glm::vec3(scale_vector))

rota

MATERIALS
-> loop thru multiple

Read STL Files for STL support -> find the algorithm for it and make it as its own script in filemanagement folder
